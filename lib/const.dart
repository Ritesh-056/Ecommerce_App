//to define the hex color in flutter we need to specify [0xff] alaways.

import 'package:flutter/material.dart';

//primaryButtonMaterial

//button color
const Color primaryButtonColor = Color(0x0ACF83);

//button text color
const Color primaryButtonTextColor = Color(0xFFFFFF);

import 'package:flutter/material.dart';


//returning the widget of horizontal space
Widget addHorizontalSpace(double hSize){
  return SizedBox(
    width: hSize,
  );
}


//returning the space of vertical space
Widget addVerticalSpace(double vSize){
  return SizedBox(
    height: vSize,
  );
}